#include "main.h"
/**
 * mul - func checks for checks for a digit (0 through).
 * @a: a - var
 * @b: b - var
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
